import pandas as pd

df = pd.DataFrame({'X': [7, 2, 0, 3, 4, 2, 5, 0, 3, 4]})
df['Y'] = df['X']
#print(df)
ar = df.Y

i = 0
dist=0
for val in ar:
    dist+=1
    if val != 0:
        ar[i] = dist
    else:
        dist = 0
    i+=1

print(df)
